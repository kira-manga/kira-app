source "https://rubygems.org"

# Pinned so TestFlight/App Store Connect behavior cannot change between workflow runs.
gem "fastlane", "2.235.0"

# Fastlane loads representable's JSON adapter at startup. Bundler 4 no longer
# exposes this implicit dependency unless it is declared explicitly.
gem "multi_json", "~> 1.15"
