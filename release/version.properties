# Human-facing store version. Update deliberately for each release train.
VERSION_NAME=1.0.5

# Local fallback for this release train. Store upload CI overrides it with the next unused code
# obtained from Google Play immediately before the signed build.
VERSION_CODE=1005
