<?xml version="1.0" encoding="utf-8"?><!--
   Kira's database, preferences, and manga files form one consistency boundary. Android Auto
   Backup cannot restore that state atomically, so exclude all app-owned state and use Kira's
   explicit .kira.zip export/import flow instead. Cache and no-backup domains are never backed up
   by Android and therefore need no entries here.
-->
<full-backup-content>
    <exclude domain="database" path="."/>
    <exclude domain="sharedpref" path="."/>
    <exclude domain="file" path="."/>
    <exclude domain="root" path="."/>
    <exclude domain="external" path="."/>
</full-backup-content>
