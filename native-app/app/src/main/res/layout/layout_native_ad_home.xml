<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"

    android:background="@color/black">

    <!-- Native Ad View -->
    <com.google.android.gms.ads.nativead.NativeAdView
        android:id="@+id/nativeAdView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"

        android:padding="2dp"
        android:visibility="invisible"> <!-- Set visibility to invisible initially for loading -->

        <!-- Ad Content Layout -->
        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <!-- AdChoicesView (Top-left corner) -->
            <com.google.android.gms.ads.nativead.AdChoicesView
                android:id="@+id/adChoice"
                android:layout_width="24dp"
                android:layout_height="24dp"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                />

            <!-- Ad Title (TextView) -->
            <TextView
                android:id="@+id/ad_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingHorizontal="4dp"
                android:paddingVertical="1dp"
                android:text="Ad"
                android:textColor="@color/white"
                android:textSize="10sp"
                android:textStyle="bold"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                tools:ignore="SmallSp" />

            <!-- Guideline (Spacing between the header and content) -->
            <androidx.constraintlayout.widget.Guideline
                android:id="@+id/guideline"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                app:layout_constraintGuide_begin="6dp" />

            <!-- Ad Image (MediaView) -->
            <com.google.android.gms.ads.nativead.MediaView
                android:id="@+id/mv_content"
                android:layout_width="120dp"
                android:layout_height="140dp"
                android:layout_margin="12dp"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <!-- App Icon (ImageView) -->
            <androidx.cardview.widget.CardView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:cardCornerRadius="8dp"
                android:layout_margin="5dp"
                android:elevation="10dp"
                android:layout_marginStart="16dp"
                android:layout_marginTop="10dp"
                android:adjustViewBounds="true"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toStartOf="@+id/btn_cta"
                app:layout_constraintStart_toEndOf="@+id/mv_content">
                <ImageView
                    android:id="@+id/iv_app_icon"
                    android:layout_width="45dp"
                    android:layout_height="45dp"
                    />
            </androidx.cardview.widget.CardView>

            <!-- Ad Headline (TextView) -->
            <TextView
                android:id="@+id/tv_headline"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="12dp"
                android:ellipsize="end"
                android:maxLines="2"
                android:textColor="@color/white"
                android:textSize="13sp"
                android:textStyle="bold"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toEndOf="@+id/mv_content"
                app:layout_constraintTop_toTopOf="@+id/mv_content"
                tools:text="Test Ad : Google Ads" />

            <!-- Ad Body (TextView) -->
            <TextView
                android:id="@+id/tv_body"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="12dp"
                android:layout_marginEnd="16dp"
                android:ellipsize="end"
                android:maxLines="2"
                android:paddingVertical="2dp"
                android:textColor="@color/white"
                android:textSize="11sp"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toEndOf="@+id/mv_content"
                app:layout_constraintTop_toBottomOf="@+id/tv_headline"
                tools:text="Stay up to date with your Ads Check how your ads are performing Check it now" />

            <!-- CTA Button (Install) -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_cta"
                android:layout_width="wrap_content"
                android:layout_height="45dp"
                android:background="@color/transport"
                android:gravity="center"
                android:textColor="?attr/colorPrimary"
                android:textSize="10sp"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toStartOf="@+id/ad_text"
                tools:text="INSTALL" />

        </androidx.constraintlayout.widget.ConstraintLayout>
    </com.google.android.gms.ads.nativead.NativeAdView>

    <!-- Shimmer Loading State (ShimmerFrameLayout) -->
    <com.facebook.shimmer.ShimmerFrameLayout
        android:id="@+id/shimmerFrameLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:orientation="vertical">

        <!-- Loading Text (Shimmer effect) -->
        <TextView
            android:id="@+id/btn_cta1"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginHorizontal="16dp"
            android:layout_marginVertical="20dp"
            android:layout_marginBottom="4dp"
            android:gravity="center"
            android:text="Loading Ad..."
            android:textColor="@color/white"
            android:textSize="16sp" />
    </com.facebook.shimmer.ShimmerFrameLayout>
</FrameLayout>
